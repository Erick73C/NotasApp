package com.erick.notasapp.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val PinkBright = Color(0xFFFB7185)   // Rosa fuerte (dark)
val PinkSoft = Color(0xFFFDA4AF)     // Rosa suave (dark)
val PinkCard = Color(0xFF27272A)     // Fondo de tarjetas (dark)

val PinkMain = Color(0xFFF43F5E)     // Rosa principal (light)
val PinkLight = Color(0xFFFB7185)    // Rosa claro (light)
val WhiteCard = Color(0xFFFFFFFF)    // Fondo tarjetas (light)
val PinkBackgroundLight = Color(0xFFFFF7F7) // Fondo principal (light)
val PinkBackgroundDark = Color(0xFF18181B)  // Fondo principal (dark)
